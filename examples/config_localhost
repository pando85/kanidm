# This should be at /etc/kubidm/config or ~/.config/kubidm, and configures the kubidm command line tool
# to point at the local dev server and trust the CA security.
uri="https://localhost:8443"
verify_ca=true
ca_path="/tmp/kubidm/ca.pem"
#!/bin/sh
# postinst script for kubidmd
#
# see: dh_installdeb(1)

set -e


case "$1" in
    configure)
        echo "Creating the kubidmd group for config & cert ownership..."
        systemd-sysusers
        echo "Fixing ownership of server configuration ..."
        chown :kubidmd /etc/kubidmd/server.toml*

        echo "============================="
        echo "Thanks for installing Kubidm!"
        echo "============================="
        echo "Please ensure you modify the configuration file at /etc/kubidmd/server.toml"
        echo "Only then: systemctl enable kubidmd.service"
        echo "Full examples are in /usr/share/kubidmd/"
    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0

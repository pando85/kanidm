[project]
name = "rlm-python"
version = "0.1.0"
description = "FreeRADIUS Kubidm module"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["kubidm"]

[dependency-groups]
dev = ["mypy>=2.3.0", "pytest>=9.1.1", "ruff>=0.16.1", "ty>=0.0.69"]

[tool.uv.sources]
kubidm = { path = "../pykubidm" }
